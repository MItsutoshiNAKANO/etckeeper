#!/bin/sh

pre_sysupgrade() {
	if [ -x /usr/bin/etckeeper ]; then
		HIGHLEVEL_PACKAGE_MANAGER=pacman-g2 etckeeper pre-install
	fi
}

post_sysupgrade() {
	if [ -x /usr/bin/etckeeper ]; then
		HIGHLEVEL_PACKAGE_MANAGER=pacman-g2 etckeeper post-install
	fi
}

op=$1
shift
$op $*
